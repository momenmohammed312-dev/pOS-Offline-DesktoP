# حل مشكلة تقارير مقارنة المبيعات بالمشتريات

## 🚨 المشكلة
عند فتح تقرير "مقارنة المبيعات بالمشتريات" يظهر خطأ قاعدة بيانات:
```
SqliteException(1): SQL logic error: no such column: totalAmount
```

## 🔧 الحلول المتاحة

### الحل 1: إصلاح تلقائي (مُوصى به)
لقد قمت بإصلاح الكود البرمجي للتقرير ليتعامل مع مشكلة الأعمدة الناقصة:

1. **فحص ديناميكي للأعمدة**: التقرير الآن يتحقق من وجود عمود `totalAmount`
2. **استبدال تلقائي**: إذا لم يتم العثور على العمود، يبحث عن بدائل مثل `total` أو `amount`
3. **رسائل تصحيح**: يظهر في الكونسول أسماء الأعمدة الفعلية الموجودة

### الحل 2: حذف قاعدة البيانات وإعادة إنشائها
إذا كانت البيانات غير مهمة:

1. **أغلق التطبيق بالكامل**
2. **احذف مجلد قاعدة البيانات**:
   ```
   %APPDATA%\pos_offline_desktop_database\
   ```
3. **أعد تشغيل التطبيق** (سيقوم بإنشاء قاعدة بيانات جديدة بالمخطط الصحيح)

### الحل 3: إصلاح يدوي باستخدام SQLite
للحفاظ على البيانات الموجودة:

1. **حمّل برنامج** DB Browser for SQLite
2. **افتح قاعدة البيانات**:
   ```
   %APPDATA%\pos_offline_desktop_database\pos_offline_desktop_database.sqlite
   ```
3. **نفذ هذه الأوامر SQL**:
   ```sql
   ALTER TABLE invoices ADD COLUMN totalAmount REAL DEFAULT 0.0;
   ALTER TABLE invoices ADD COLUMN paidAmount REAL DEFAULT 0.0;
   ALTER TABLE purchases ADD COLUMN totalAmount REAL DEFAULT 0.0;
   ```
4. **احفظ قاعدة البيانات وأغلق البرنامج**
5. **أعد تشغيل تطبيق POS**

## 📋 التغييرات التي تم إجراؤها

### 1. تحديث ملف قاعدة البيانات (`app_database.dart`)
- إضافة ترحيل تلقائي لأعمدة `totalAmount` و `paidAmount`
- التأكد من وجود الأعمدة عند ترقية قاعدة البيانات

### 2. تحديث تقرير المقارنة (`purchase_vs_sales_report.dart`)
- فحص ديناميكي لأسماء الأعمدة
- استخدام بديل إذا كان العمود المطلوب غير موجود
- رسائل تصحيح مفيدة في الكونسول

## 🎯 التوصية

**استخدم الحل 1 (الإصلاح التلقائي)** لأنه:
- ✅ لا يتطلب تدخل يدوي
- ✅ يحافظ على البيانات الموجودة
- ✅ يعمل مع جميع قواعد البيانات (القديمة والجديدة)
- ✅ يوفر رسائل تصحيح مفيدة

## 🔄 الخطوات التالية

1. **أعد تشغيل تطبيق POS**
2. **افتح تقرير مقارنة المبيعات بالمشتريات**
3. **تحقق من الكونسول** لرؤية رسائل التصحيح
4. **إذا استمرت المشكلة**، استخدم الحل 2 أو 3

## 📞 المساعدة

إذا استمرت المشكلة:
1. شاهد الكونسول لرسائل التصحيح
2. تحقق من أسماء الأعمدة الفعلية في قاعدة البيانات
3. تواصل مع الدعم الفني للمساعدة

---
**تم التحديث:** 5 فبراير 2026
**الإصدار:** 2.0.0
